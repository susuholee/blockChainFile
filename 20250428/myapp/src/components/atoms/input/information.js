import React from "react";

const InformationInput = ({ width, height }) => {
  return (
    <div className="information-box">
      <input type="" style={{ width: width, height: height }} className="information-input" 
      />
    </div>
  );
};

export default InformationInput;
